import React from 'react'

/** Brand mark for Payload admin Better Auth login / first-admin views (replaces plugin default). */
export const AdminAuthLogo: React.FC = () => {
  return (
    <div className="admin-auth-brand mb-6 flex w-full justify-center">
      <span className="text-xl font-semibold tracking-tight text-neutral-200 sm:text-2xl">
        Butchers <span className="text-[#f87171]">Craft</span>
      </span>
    </div>
  )
}
