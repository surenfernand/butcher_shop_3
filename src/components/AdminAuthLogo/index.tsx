import React from 'react'

/** Brand mark for Payload admin Better Auth login / first-admin views (replaces plugin default). */
export const AdminAuthLogo: React.FC = () => {
  return (
    <div className="admin-auth-brand mb-8 flex w-full justify-center">
      <span className="text-2xl font-semibold tracking-tight text-neutral-900">
        Butchers <span className="text-[#D32F2F]">Craft</span>
      </span>
    </div>
  )
}
